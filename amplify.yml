version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist/public  # ← Add this line (was missing or set to 'dist')
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
